<div class="container justify-content-center">
    <div class="row">
        {{#each workout}}
        <div class="col-3">
            <div class="card" style="width: 18rem;">
                <h1 class="card-header">
                    {{name}}
                </h1>
                {{!-- {{#each exercises}}
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">{{name}}</li>
                    <li class="list-group-item">{{type}}</li>
                    <li class="list-group-item">{{weight}}</li>
                    <li class="list-group-item">{{sets}}</li>
                    <li class="list-group-item">{{reps}}</li>
                    <li class="list-group-item">{{duration}}</li>
                </ul>
                <button data-id={{id}} class="btn btn-secondary update-workout" data-id="_id">Update Workout</button>
                <button class="btn btn-danger delete-workout" data-id="_id">Delete Workout</button>
                <button class="btn btn-primary start" data-id="_id">Start Session</button>
                {{/each}} --}}
            </div>
        </div>
        {{/each}}
    </div>